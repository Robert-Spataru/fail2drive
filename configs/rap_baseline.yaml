# RAP baseline config
name: rap_baseline
system: rap
mode: baseline
camera_count: 2
parameters:
  fps: 30
  resolution: [1920, 1080]
  timeout_seconds: 60
  note: "Baseline settings for RAP. Adjust as required."
