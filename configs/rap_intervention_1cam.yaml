# RAP intervention config (1 camera)
name: rap_intervention_1cam
system: rap
mode: intervention
camera_count: 1
parameters:
  fps: 30
  resolution: [1280, 720]
  timeout_seconds: 60
  note: "Placeholder config — update fields as needed."
